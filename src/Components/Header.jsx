import React from 'react';
import styled from 'styled-components';
const Header = () => {
  return <StHeader>Heloo</StHeader>;
};

export default Header;

const StHeader = styled.header`
  height: 40px;
  background-color: #bada55;
`;
